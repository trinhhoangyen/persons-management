services:
  - type: web
    name: spring-backend
    env: java
    plan: free
    rootDir: back-end
    buildCommand: ./mvnw clean package
    startCommand: java -jar target/*.jar
    envVars:
      - key: PORT
        value: 8080
